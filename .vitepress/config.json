import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})
import { defineConfig } from 'vitepress'
import { SearchPlugin } from 'vitepress-plugin-search'

// flexsearch 可选配置（支持中文）
const searchOptions = {
  // 可调整索引/分词设置，flexsearch 支持多语言
  encode: "icase",
  tokenize: "forward",
  threshold: 0,
  resolution: 9,
  depth: 3
}

export default defineConfig({
  base: '/wiki/',          // ⚠️ 非常重要：部署在 /wiki 下
  lang: 'zh-CN',
  title: '项目 Wiki',
  description: '项目文档',
  themeConfig: {
    search: {
      // VitePress 自带的 local search（minisearch）可用：
      // provider: 'local'
      // 但为了更好地支持中文，推荐使用下方 flexsearch 插件（本 repo 已加入）
      provider: 'local'
    },
    // 其它主题配置
    nav: [
      { text: '首页', link: '/' },
      { text: '指南', link: '/guide' }
    ],
    // 启用暗黑模式
    darkMode: true
  },
  vite: {
    plugins: [
      SearchPlugin({
        ...searchOptions,
        previewLength: 62,
        placeholder: "在文档中搜索（支持中文）"
      })
    ]
  }
})

